package com.acb.libwallpaper.live.dialog;

/**
 * Interface for views that are added to a float window.
 */
public interface FloatWindowListener {
    void onAddedToWindow(SafeWindowManager windowManager);
}
